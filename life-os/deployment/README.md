# Mac mini 部署

呢個 folder 入面：

- `com.kelvin.lifeos.plist` — launchd service definition，由 `scripts/install-launchd.sh` 安裝
- `Caddyfile` — Caddy reverse proxy 配置（記得改 Tailscale hostname）

詳細設置步驟見 [`../docs/setup-mac-mini.md`](../docs/setup-mac-mini.md)。
