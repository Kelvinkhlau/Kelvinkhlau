# Copy to .env. Used by src/services/apiClient.js.
# In dev, leave empty to use Vite's /api proxy (see vite.config.js).
# In production, set to the API origin, e.g. http://100.x.x.x:8080
VITE_API_BASE_URL=
